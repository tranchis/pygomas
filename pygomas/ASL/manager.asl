
+name(X) <- 
  .print("In ASL,setting name to:");
  .print(X);
  -name(_).

+x(X) <- 
  .print("In ASL,setting x to:");
  .print(X);
  -name(_).

+y(X) <- 
  .print("In ASL,setting y to:");
  .print(X);
  -name(_).



